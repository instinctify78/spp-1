# Copy to .env and adjust values
DATABASE_URL=sqlite:///./data/runs.db
REDIS_URL=redis://localhost:6379/0
DATA_DIR=./data

# For NVIDIA multi-GPU: set which GPU the worker sees
# CUDA_VISIBLE_DEVICES=0
